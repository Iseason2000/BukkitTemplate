name: ${name}
main: ${main}
version: ${version}
authors: [ ${ author } ]
#api-version: 1.16
runtime-libraries:
  libraries-folder: '@Server:libraries' # If not defined, it will be 'libs'
  libraries:
    kotlin:
      groupId: org.jetbrains.kotlin
      artifactId: kotlin-stdlib
      version: ${kotlinVersion}